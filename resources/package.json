{
  "name": "lambda-cognito-access-token-enricher",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.159",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node24 --outdir=dist"
  }
}
